# Optional Flask settings
FLASK_APP=webapp.app
FLASK_ENV=production
PORT=5000

# Optional email overrides (if using email_report.py)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_TO=
